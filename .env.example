# Copy this to `.env` (do NOT commit your real keys)
# and restart the server. Alternatively, set these in your PowerShell before
# launching `uvicorn` from the same terminal session.

# LLM provider settings
LLM_PROVIDER=groq
GROQ_API_KEY=your_groq_api_key_here
MODEL_NAME=llama-3.3-70b-versatile
REQUIRE_PROVIDER=true

# Retrieval defaults
ENABLE_BM25=auto
BM25_FILES_DIR=articles
# BM25 top-k per run can also be set in the request body via bm25_k
